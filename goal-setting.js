if (_currentViewId==="goals-thankyou"){
  // TODO: add the summary using getRadioText(name of element) and getValue()
  let h = "<ul><li>The summary can go here.</li></ul>";
  $("#goal-summary").html(h);
}